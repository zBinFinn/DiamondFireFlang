mod std.player;

singleton Player {
    @OnPlayerSelection
    internal fn sendMessage(text: Any);

    @OnPlayerSelection
    internal fn showActionBar(text: Text);

    @OnPlayerSelection
    fn getName(): Text {
        return gval("Name", .SELECTION);
    }
}